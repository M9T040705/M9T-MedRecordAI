# 核心依赖（不含 OCR，轻量可跑）
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
python-dotenv>=1.0
openai>=1.30
httpx>=0.27
numpy>=1.26
pymysql>=1.1
pytest>=8.0
pyjwt>=2.8
python-multipart>=0.0.9

# 文档解析
pymupdf>=1.24
python-docx>=1.1
openpyxl>=3.1
python-pptx>=0.6.23
Pillow>=10.0

# 样例生成（reportlab 造 PDF）
reportlab>=4.0

# ---- OCR（可选，重依赖，单独装）----
# pip install paddlepaddle paddleocr
# 参考 requirements-ocr.txt
